// import { shallow } from 'enzyme';
// import * as React from 'react';
// import ResultItem from '../components/playgame/ResultItem';
// describe('Player Result Item', () => {
//     const noop = () => {};
//     it('triggers onClick event handler when clicked', () => {
//       const onClick = jest.fn();
//       const wrapper = shallow(<Login onClick={onClick} open={false} />).simulate('click');
//       wrapper.find(Bar)
//       expect(onClick).toBeCalledTimes(1);
//     });
//   });
